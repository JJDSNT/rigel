#ifndef RIGEL_AGNUS_TIMING_BEAM_H
#define RIGEL_AGNUS_TIMING_BEAM_H

/* TODO(timing): canonical location for beam position types and API.
 * Content currently lives in agnus/beam.{h,c}; migrate here when the
 * timing/ sub-module is complete and update agnus_state.h include path. */
#include "agnus/beam.h"

#endif
